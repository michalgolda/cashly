import { Spendings, SpendCategories } from "./pages"

const routes = [
    {
        path: "/spendings",
        component: Spendings
    },
    {
        path: "/categories",
        component: SpendCategories
    }
];

export default routes;